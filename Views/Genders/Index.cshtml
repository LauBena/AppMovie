@model IEnumerable<AppMovie.Models.Gender>

@{
    Layout = "_Layout";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Nuevo Género</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GenderName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GenderName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GenderID">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.GenderID">Detalles</a> |
                <a onclick="eliminarSeccion(@item.GenderID);" class="btn btn-danger">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>

<script>
    function eliminarSeccion(id){
        var eliminar = confirm("Esta seguro que desea eliminar esta seccion?");
        if(eliminar){
            location.href = "../../Genders/DeleteConfirmed/" + id;
        }
    }
</script>
</body>
</html>
