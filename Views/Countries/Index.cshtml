@model IEnumerable<AppMovie.Models.Country>

@{
    Layout = "_Layout";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Nuevo País</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CountryName)
            </th>
            @* <th>
                @Html.DisplayNameFor(model => model.LocalidadID)
            </th> *@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CountryName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CountryID">Editar</a> |
                <a asp-action="Details" asp-route-id="@item.CountryID">Detalles</a> |
                <a onclick="eliminarSeccion(@item.CountryID);" class="btn btn-danger">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>

<script>
    function eliminarSeccion(id){
        var eliminar = confirm("Esta seguro que desea eliminar esta seccion?");
        if(eliminar){
            location.href = "../../Countries/DeleteConfirmed/" + id;
        }
    }
</script>
</body>
</html>
